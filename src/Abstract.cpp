#include <iostream>

#include "Abstract.hpp"
#include "Term.hpp"
#include "Polynomial.hpp"

/*Abstract::operator Term() const {
    return Term(*this);
}*/

/*Abstract::operator Polynomial() {
    return Polynomial(*this);
}
*/

size_t Abstract::cnt = 0;
